---
name: Check file name case

on:
  pull_request:
    branches:
      - main

jobs:
  case:
    name: Check for licenses not being allowed
    runs-on: ubuntu-latest
    strategy:
      matrix:
        folder: ["docs"]
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v3

      - name: Dependency review
        uses: ConsenSys/github-actions/js-dependency-review@ec3b452d3471664332de10917cae9e6e19415eab # main
        with:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
